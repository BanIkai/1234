#pragma once
#include "Sensors.h"

// ============================================================
//  Strategy.h — ประกาศ API ของ AI หุ่นยนต์
//
//  AI ตัดสินใจตามลำดับความสำคัญดังนี้:
//
//    1. หนีเส้น   — ถ้าจะออกนอกสนาม → ถอยแล้วหมุนกลับ (ด่วน!)
//    2. โจมตี     — ถ้าศัตรูอยู่ใกล้ → พุ่งชนเต็มสปีด
//    3. ติดตาม    — ถ้าเห็นศัตรูแต่ยังไกล → เลี้ยวตาม
//    4. ค้นหา     — ถ้าไม่เห็นศัตรูเลย → หมุนหาแบบ spiral
// ============================================================

namespace AI {

  // ล้างค่าทั้งหมดให้เริ่มต้นใหม่ — เรียกใน setup()
  void reset();

  // อัปเดต AI 1 รอบ — เรียกทุก loop()
  // รับข้อมูล sensor แล้วสั่งมอเตอร์
  void run(Dist dist, Line line);

}
