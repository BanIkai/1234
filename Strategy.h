#pragma once
#include "Sensors.h"

// ============================================================
//  Strategy.h — AI หลักของหุ่น
//
//  ลำดับความสำคัญ (priority):
//    1. หนีเส้น   — ปลอดภัยก่อนเสมอ
//    2. โจมตี     — เจอศัตรูในระยะพุ่ง
//    3. ติดตาม    — เจอศัตรูแต่ยังไกล
//    4. ค้นหา     — ยังไม่เจอศัตรู
// ============================================================

namespace AI {

  // เรียกใน setup() เพื่อ reset state ทั้งหมด
  void reset();

  // เรียกทุก loop() เพื่ออัปเดตการตัดสินใจ
  void run(Dist dist, Line line);
}
