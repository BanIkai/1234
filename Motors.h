#pragma once

// ============================================================
//  Motors.h — ควบคุมมอเตอร์ซ้าย/ขวา
//  ค่าความเร็ว: +255 = หน้าเต็ม, -255 = ถอยเต็ม, 0 = หยุด
// ============================================================

namespace Motors {

  // เริ่มต้น pin ทั้งหมด (เรียกใน setup() ครั้งเดียว)
  void begin();

  // สั่งความเร็วมอเตอร์ซ้าย/ขวา (-255 ถึง +255)
  void move(int leftSpeed, int rightSpeed);

  // หยุดทั้งสองมอเตอร์
  void stop();
}
